
import "imports/game_predicates.onjschema" as _;
import "imports/textEffectEmitterConfig.onjschema" as textEmitterConfig;

use Common;
use Game;

var action = {
    weight: EnemyActionValue,
    action: EnemyAction?,
    maxExecutions?: int,
    selectionConditions?: EnemyPredicate[],
    variants?: {
        weight: int,
        hidden?: boolean,
    }[]
};

var enemy = {
    name: string,
    title: string,
    baseHealth: int,
    texture: string,
    actions: action[],
};

enemies: enemy[],

enemyGroups: {
    groupName: string,
    variants: {
        name: string,
        majorDifficulty: int,
    }[]
}[],
