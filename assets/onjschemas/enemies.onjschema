
use Common;

var enemy = {
    name: string,
    baseHealthPerTurn: int,
    baseDamage: int,
    fightDuration: int[2],
    texture: string,
    coverIcon: string,
    scaleX: float,
    scaleY: float,
    coverIconScale: float,
    detailFont: string,
    detailFontScale: float,
    detailFontColor: Color,
};

enemies: enemy[]
