//import "imports/map_events.onjschema" as _;
// TODO: ^ this needs to be fixed in onj
import "imports/run.onjschema" as run;

var tutorialText = {
    text: string,
    confirmationText: string,
    focusActor?: string,
    triggerOnNodes: int[],
    highlightObject?: string,
};

majorDifficulty: int,
tutorialText?: tutorialText[],
startNode: int,
endNode: int,
nodes: {
    x: float,
    y: float,
    edgesTo: int[],
    image?: string,
    imagePos?: string,
    event: $MapEvent?,
    distance?: int,
    nodeTexture?: string,
}[],

decorations: {
    texture: string,
    baseWidth: float,
    baseHeight: float,
    positions: float[3][],
    drawInBackground?: boolean,

}[],
animatedDecorations: {
    texture: string,
    baseWidth: float,
    baseHeight: float,
    positions: float[3][],
    drawInBackground?: boolean, // TODO: workaround, this key doesn't actually do anything
}[],
biome: string,
scrollable?: boolean,
camPosOffset?: float[2],

areaConfig?: {
    progressRun?: run,
    completedProgressRun: boolean
}
