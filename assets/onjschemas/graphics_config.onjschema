
use Common;

icons: {
    burning: string,
    poison: string,
    fireResistance: string,
    bewitched: string,
    shield: string,
    frozen: string,
    weak: string,
    bounty: string,
},

var encounterModifierConfig = {
    icon: string,
    displayName: string,
    description: string,
};

encounterModifiers: {
    rain: encounterModifierConfig,
    frost: encounterModifierConfig,
    bewitchedMist: encounterModifierConfig,
    lookalike: encounterModifierConfig,
    moist: encounterModifierConfig,
    steelNerves: encounterModifierConfig,
    draft: encounterModifierConfig,
    anOfferYouCantRefuse: encounterModifierConfig,
    bulletSkipping: encounterModifierConfig,
    sacrifice: encounterModifierConfig,
    sorryNotSorry: encounterModifierConfig,
    confused: encounterModifierConfig,
},

encounterBackgrounds: {
    biome: string,
    isDark: boolean,
    background: string,
    secondaryBackground: string,
}[],

damageOverlay: {
    duration: float,
    fadeIn: float,
    fadeOut: float,
    overlay: string
},

cardText: {
    font: string,
    fontScale: float,
    colorsForDarkCard: {
        normal: Color,
        increase: Color,
        decrease: Color,
    },
    colorsForLightCard: {
        normal: Color,
        increase: Color,
        decrease: Color,
    },
},


revolverSlotIcons: {
    "1": string,
    "2": string,
    "3": string,
    "4": string,
    "5": string,
},
