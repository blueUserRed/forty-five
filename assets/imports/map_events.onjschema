import "imports/map_predicates.onjschema" as _;

$DifficultyScaling {
    $LinearScaling {}
    $PowerScaling {
        power: int,
    }
}

var mapEventStandardConfig = {
    currentlyBlocks: boolean,
    startable: boolean,
    isCompleted: boolean,
    startConditions?: $MapPredicate[],
    blockConditions?: $MapPredicate[],
};

var encounter = {
    enemies: string[],
    encounterModifier: string[],
    forceCards: string[]?,
    shuffleCards: boolean,
    unadjustedMajorDifficulty: int,
    majorDifficulty: int,
    minorDifficulty: float,
    difficultyScalingInfo?: float,
    special: boolean,
};

$MapEvent {

    $EmptyMapEvent { }

    $SimpleMapEvent {
        ...mapEventStandardConfig,
        displayDescription?: boolean,
        descriptionText?: string,
        displayName?: string
    }

    $EncounterMapEvent {
        ...mapEventStandardConfig,
        encounter: encounter,
        isExtraction: boolean
    }

    $EnterMapMapEvent {
        targetMap: string,
        fromEnd: Boolean,
    }

    $DialogMapEvent {
        dialog: string,
        displayName: string,
        descriptionText: string,
        completedDescriptionText: string,
        canOnlyBeStartedOnce: boolean,
        ...mapEventStandardConfig
    }

    $ShopMapEvent {
        types: string[],
        person: string,
        amountCards: int[2],
        currentCards: string[]?,
        amountOfRerolls: int,
        rerollPriceIncrease: int,
        rerollBasePrice: int,
        boughtIndices: int[],
    }

    $ChooseCardMapEvent {
        types: string[],
        seed: int?,
        nbrOfCards: int,
        amountOfRerolls: int,
        rerollPriceIncrease: int,
        rerollBasePrice: int,
        enableRerolls: boolean,
        ...mapEventStandardConfig
    }

    $LockNodeMapEvent {
        conditions: $MapPredicate[],
        locked: boolean,
        lockedDescription: string,
        openDescription: string,
    }

    $CompleteRunMapEvent {
        runName: string,
        displayName: string,
        descriptionText: string,
        completedDescriptionText: string,
        ...mapEventStandardConfig
    }

    $FinishTutorialRunMapEvent { }

    $EncounterPlaceholderMapEvent {
        genExtraction: boolean,
        majorDifficulty: int,
        unadjustedMajorDifficulty: int,
        minorDifficulty: float,
        runModifier: string[],
        amountEnemies: int[2],
        difficultyScaling: $DifficultyScaling,
        scaleMin: float,
        scaleMax: float,
        biome: string,
        seed: int
    }

}
